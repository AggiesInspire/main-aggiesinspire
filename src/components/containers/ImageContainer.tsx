/* content container for display */
import React from "react";

type Props = {
  image?: any;
  title: string;
  content: string;
  link?: any;
};

const Container = ({ title, content, image, link }: Props) => {
  return (
    <div>
      <div></div>
    </div>
  );
};
